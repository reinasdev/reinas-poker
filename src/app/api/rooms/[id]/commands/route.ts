import { NextResponse } from "next/server";import { requireUser } from "@/application/auth";import { addTask,castVote,completeTask,editTask,finishRoom,removeTask,reorderTasks,restartRound,revealVotes } from "@/application/rooms";import { apiError,assertSameOrigin } from "@/app/api/_shared";
export async function POST(request:Request,{params}:{params:Promise<{id:string}>}){try{assertSameOrigin(request);const user=await requireUser();const roomId=(await params).id;const body=await request.json();switch(body.action){case"task.add":await addTask(user.id,roomId,body.title,body.link);break;case"task.edit":await editTask(user.id,roomId,body.taskId,body.title,body.link);break;case"task.remove":await removeTask(user.id,roomId,body.taskId);break;case"task.reorder":await reorderTasks(user.id,roomId,body.ids);break;case"vote.cast":await castVote(user.id,roomId,body.value);break;case"round.reveal":await revealVotes(user.id,roomId);break;case"round.restart":await restartRound(user.id,roomId);break;case"task.complete":await completeTask(user.id,roomId,body.result);break;case"room.finish":await finishRoom(user.id,roomId);break;default:return NextResponse.json({message:"Comando inválido"},{status:400})}return NextResponse.json({ok:true})}catch(e){return apiError(e)}}
