import { NextResponse } from "next/server";import { requireUser } from "@/application/auth";import { joinRoom } from "@/application/rooms";import { apiError,assertSameOrigin } from "@/app/api/_shared";import { assertRateLimit } from "@/infrastructure/security/rate-limit";
export async function POST(request:Request,{params}:{params:Promise<{id:string}>}){try{assertSameOrigin(request);const user=await requireUser();const {id}=await params;assertRateLimit(`room:${id}:${user.id}`,10,60_000);const {password}=await request.json();await joinRoom(user.id,id,password);return NextResponse.json({ok:true})}catch(e){return apiError(e)}}
